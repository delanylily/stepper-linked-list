// import { createAction, props } from "@ngrx/store";

// export const updateUser = createAction('[User details] update user', props<{ userId: string, user: any; }>());
// export const userUpdated = createAction('[User details Updated] user updated', props<{ user: any; }>());
